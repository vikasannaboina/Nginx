pipeline{
    agent any
    stages{
        stage("GIT-Checkout"){
            steps{
               git branch: 'main', credentialsId: 'github', url: 'https://github.com/vikasannaboina/Nginx.git'
            }
        }
    }
}
